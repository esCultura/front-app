import { Text } from "react-native";
import Screen from "../components/Screen";

export default function Chat(props) {
    return (
        <Screen>
            <Text>CHAT</Text>
        </Screen>
    );
}